---
import { projects } from "../Projects.astro";

interface Props {
  project: string;
}
const { project } = Astro.props;

const p = projects[project];
---

<section class="w-[32%] border border-typography bg-base rounded-md p-2">
  <h2>{p.title}</h2>
  <span>{p.date}</span>
</section>
